for(int mask = 0 ; mask < (1 << sz(s)) ; ++mask) {
        int cur = 0;
        for(int i = 0 ; i < sz(s); ++i) {
            if(mask & (1 << i)) {
                
            }
        }
        // do operation
    }
